{
  "name": "geco-tests",
  "version": "1.0.0",
  "description": "Cypress tests for the GECO app",
  "author": "Doug Waldron",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^8.5.0"
  },
  "scripts": {
    "dev": "cypress open --env configFile=dev",
    "dev-run": "cypress run --env configFile=dev",
    "uat": "cypress open --env configFile=uat",
    "uat-run": "cypress run --env configFile=uat",
    "prod": "cypress open --env configFile=prod",
    "prod-run": "cypress run --env configFile=prod",
    "dr": "cypress run --env configFile=dr"
  }
}